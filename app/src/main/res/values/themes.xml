<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Splash theme -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_screen</item>
        <item name="windowSplashScreenAnimationDuration">500</item>
        <item name="postSplashScreenTheme">@style/Theme.AMC</item>
    </style>

    <!-- Real app theme (no ActionBar, no window title) -->
    <style name="Theme.AMC" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/purple_500</item>  <!-- removes window title -->
        <item name="android:windowActionBar">false</item>
        <item name="android:statusBarColor">#FF0000</item> <!-- debug color -->
        <item name="android:windowNoTitle">true</item>    <!-- removes window title -->
    </style>

</resources>